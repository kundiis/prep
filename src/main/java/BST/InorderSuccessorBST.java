package BST;

public class InorderSuccessorBST {

    /*
    Start with root, current = root, successor = NULL.
    If node.value < current.value, then successor = current, current = current.left.
    If node.value > current.value, current = current.right.
    If node.value == current.value and node.right != null, successor = minimum(current.right).
    Return successor

     */

}
